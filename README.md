About The Porject(Shoppingcenternetwork):



SHEMA.SQL:
Originally, suppliers were modeled as directly connected to products. In a more realistic architecture, suppliers would be linked to a centralized warehouse layer, which is partially introduced in this project as a future extension.

INDEX.SQL:
Indexes were designed based on expected query patterns, including joins, time-based filtering, and aggregation use cases.